# Sourcehut manifest.
image: freebsd/latest
packages:
- cmake
sources:
- https://github.com/pmer/TsunagariC.git
tasks:
- cmake: |
    mkdir build
    cd build
    cmake ..
- make: |
    cd build
    make
