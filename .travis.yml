language: cpp

#matrix:
#  - os: linux
#    dist: xenial
os: osx
osx_image: xcode10.1

addons:
  apt:
    sources:
      - sourceline: "deb http://mirrors.kernel.org/ubuntu/ disco main"
    packages:
      - cmake
  homebrew:
    packages:
      - cmake

script:
  - mkdir build
  - cd build
  - cmake ..
  - cmake --build .
